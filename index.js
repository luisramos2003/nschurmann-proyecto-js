const fn = () => {
    const miString = "string de prueba"
    let b = miString + "string funciono con exito"
    console.log(b)
}

fn()

const clickeado = () => {
    let a = "Texto de ejemplo"

    console.log(a)
}